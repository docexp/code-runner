---
spec: "005"
chunk: "001"
title: "Add GitHub Actions CI workflow"
status: "done"
---

# Chunk 001: Add GitHub Actions CI workflow

## Scope of This Chunk

Implements the full CI pipeline as specified in `specs/005-000-ci.md`:

- Create `.github/workflows/ci.yml` with `typecheck`, `unit-test`, and `e2e` jobs
- Update `.github/workflows/auto-github-pr.yml` to target `next` instead of `main`

Nothing is deferred — this is the only chunk for spec 005.

## Pre-Implementation Checklist

- [x] Parent spec is `status: approved`
- [x] `bunx nx run-many -t build --output-style=stream` passes before starting
- [x] `bunx nx run-many -t test --output-style=stream` passes before starting

## Implementation Plan

1. Create `.github/workflows/ci.yml` with three jobs: `typecheck`, `unit-test`, `e2e`
2. Edit `.github/workflows/auto-github-pr.yml`: change `target_branch: main` → `target_branch: next`
3. Verify build + typecheck + tests all pass

## Files Changed

| File | Change |
|---|---|
| `.github/workflows/ci.yml` | Created |
| `.github/workflows/auto-github-pr.yml` | Changed `target_branch` to `next` |

## Post-Implementation Checklist

- [x] `bunx nx run-many -t build --output-style=stream` passes
- [x] No TypeScript errors (`bunx nx run-many -t typecheck --output-style=stream`)
- [x] Acceptance criteria from parent spec satisfied for this chunk
- [x] Chunk status updated to `done`
